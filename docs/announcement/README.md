# The bitcointalk announcement

`blockyard-announcement.txt` is the post in Markdown; `blockyard-announcement.bbcode` is the same
post in BBCode for the forum. The four `IMAGE-URL-FOR:<file>.jpg` placeholders take the image
host's URLs once the pictures here are uploaded; the other screenshots are here too, for the
thread.

Posted 2026-09-14: https://bitcointalk.org/index.php?topic=5594141.msg67144312
